import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SexualDysfunctionsParaphiliasPage } from './sexual-dysfunctions-paraphilias.page';

describe('SexualDysfunctionsParaphiliasPage', () => {
  let component: SexualDysfunctionsParaphiliasPage;
  let fixture: ComponentFixture<SexualDysfunctionsParaphiliasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SexualDysfunctionsParaphiliasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
